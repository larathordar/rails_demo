
<%= link_to 'new article', 'articles/new'  %>


<% @articles.each do |article| %>
<%= link_to article.title, article_path(article) %>
  <p><%= article.content %></p>
  <p>by <%= article.author %>
  at <%= article.created_at.strftime('%Y-%m-%d') %>
  </p>

<% end %>
